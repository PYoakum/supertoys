{
  "name": "bun-task-scheduler",
  "version": "1.0.0",
  "description": "A task scheduler for Bun that executes functions based on JSON configuration",
  "type": "module",
  "scripts": {
    "start": "bun run src/scheduler.ts",
    "dev": "bun --watch src/scheduler.ts"
  },
  "dependencies": {
    "cron-parser": "^4.9.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
